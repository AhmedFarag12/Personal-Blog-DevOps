apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: personal-blog
spec:
  type: NodePort
  selector:
    app: personal-blog
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
